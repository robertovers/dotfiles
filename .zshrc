
eval "$(starship init zsh)"
eval "$(devbox global shellenv)"

alias nv="nvim"
alias lg="lazygit"
alias tm="tmuxp load ~/.config/tmuxp/main.yaml"

# The next line was added by hotel, leave it at the bottom of this file
source /Users/robbie.overs/.config/hotel/config.zsh
